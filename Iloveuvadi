<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>I Love U Vadii</title>
<style>
    body {
        font-family: 'Segoe UI', sans-serif;
        background: linear-gradient(135deg, #ff758c, #ff7eb3);
        margin: 0;
        padding: 0;
        text-align: center;
        color: white;
        overflow: hidden;
    }
    header {
        padding: 50px 20px;
        z-index: 2;
        position: relative;
    }
    h1 {
        font-size: 3.5rem;
        margin-bottom: 10px;
        text-shadow: 2px 2px 5px rgba(0,0,0,0.3);
        animation: fadeIn 2s ease-in-out;
    }
    p {
        font-size: 1.2rem;
        max-width: 700px;
        margin: auto;
        animation: fadeIn 3s ease-in-out;
    }
    .proposal-box {
        background: rgba(255, 255, 255, 0.15);
        padding: 25px;
        border-radius: 20px;
        margin: 40px auto;
        width: 80%;
        max-width: 600px;
        box-shadow: 0 0 20px rgba(0,0,0,0.3);
        animation: popUp 2s ease-in-out;
        position: relative;
        z-index: 2;
    }
    .heart {
        font-size: 5rem;
        animation: beat 1s infinite alternate;
    }
    button {
        background: #ff4f70;
        border: none;
        padding: 15px 30px;
        font-size: 1.2rem;
        color: #fff;
        border-radius: 25px;
        cursor: pointer;
        transition: background 0.3s ease;
    }
    button:hover {
        background: #ff2d5a;
    }
    /* Floating hearts */
    .floating-heart {
        position: absolute;
        bottom: -50px;
        color: rgba(255,255,255,0.7);
        font-size: 2rem;
        animation: floatUp 5s linear infinite;
    }
    @keyframes beat {
        to { transform: scale(1.2); }
    }
    @keyframes fadeIn {
        from {opacity: 0;}
        to {opacity: 1;}
    }
    @keyframes popUp {
        from {transform: scale(0.5); opacity: 0;}
        to {transform: scale(1); opacity: 1;}
    }
    @keyframes floatUp {
        0% { transform: translateY(0) scale(1); opacity: 1; }
        100% { transform: translateY(-800px) scale(1.5); opacity: 0; }
    }
</style>
</head>
<body>

<audio autoplay loop>
    <source src="https://www.bensound.com/bensound-music/bensound-romantic.mp3" type="audio/mpeg">
</audio>

<header>
    <h1>I Love U, Vadii ❤️</h1>
    <p>From the moment I met you, my heart whispered your name.  
    You are my sunshine, my safe place, and my forever dream.  
    Today, I want to ask you something that will change my life…</p>
</header>

<div class="proposal-box">
    <div class="heart">❤️</div>
    <h2>Will You Be Mine Forever?</h2>
    <p>Vadii, I promise to love you endlessly,  
    to hold you in every storm,  
    and to make you smile for the rest of my life. 💖</p>
    <button onclick="alert('Yay! You said YES! ❤️')">Yes, I Will!</button>
</div>

<!-- Floating hearts generator -->
<script>
function createHeart(){
    const heart = document.createElement('div');
    heart.classList.add('floating-heart');
    heart.innerHTML = '❤️';
    heart.style.left = Math.random() * window.innerWidth + 'px';
    heart.style.animationDuration = (3 + Math.random() * 3) + 's';
    document.body.appendChild(heart);
    setTimeout(() => heart.remove(), 6000);
}
setInterval(createHeart, 300);
</script>

</body>
</html>
